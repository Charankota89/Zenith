# Built outputs
*.apk
*.aab
*.ap_
*.dex

# Build folders
build/
app/build/
.gradle/
.idea/
*.iml

# Local properties (never commit SDK path)
local.properties

# Android Studio / IntelliJ
*.iws
*.ipr
out/
captures/
.externalNativeBuild/
.cxx/

# Keystore (never commit *release* signing keys)
*.jks
*.keystore
# Exception: the fixed debug keystore is intentionally committed so CI/local
# builds share a stable SHA-1 for Google Sign-In. It uses Android's standard
# public debug password ("android") — it is not a secret.
!app/debug.keystore

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
