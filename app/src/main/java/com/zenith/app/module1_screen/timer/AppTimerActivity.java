package com.zenith.app.module1_screen.timer;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

import com.zenith.app.R;

/**
 * AppTimerActivity — lets the user configure daily app time limits.
 * Stub: full implementation added in future sprint.
 */
public class AppTimerActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // TODO: inflate layout when feature is implemented
        setContentView(R.layout.fragment_placeholder);
    }
}
